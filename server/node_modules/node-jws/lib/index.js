"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.desanitizeBase64 = exports.sanitizeBase64 = exports.JWTAlghoritm = void 0;
var JWTAlghoritm;
(function (JWTAlghoritm) {
    JWTAlghoritm["RS256"] = "RS256";
    JWTAlghoritm["RS384"] = "RS384";
    JWTAlghoritm["RS512"] = "RS512";
    JWTAlghoritm["PS256"] = "PS256";
    JWTAlghoritm["PS384"] = "PS384";
    JWTAlghoritm["PS512"] = "PS512";
    JWTAlghoritm["ES256"] = "ES256";
    JWTAlghoritm["ES384"] = "ES384";
    JWTAlghoritm["ES512"] = "ES512";
    JWTAlghoritm["HS256"] = "HS256";
    JWTAlghoritm["HS384"] = "HS384";
    JWTAlghoritm["HS512"] = "HS512";
    JWTAlghoritm["none"] = "none";
})(JWTAlghoritm = exports.JWTAlghoritm || (exports.JWTAlghoritm = {}));
function sanitizeBase64(base64) {
    return base64.replace(/=/g, '').replace(/\+/g, '-').replace(/\//g, '_');
}
exports.sanitizeBase64 = sanitizeBase64;
function desanitizeBase64(base64) {
    return base64.replace(/=/g, '').replace(/-/g, '+').replace(/_/g, '/');
}
exports.desanitizeBase64 = desanitizeBase64;
var JWS = /** @class */ (function () {
    function JWS(provider) {
        this.header = { alg: JWTAlghoritm.none };
        this.claims = {};
        this.provider = provider;
    }
    JWS.prototype.expiresIn = function (sec) {
        this.claims.exp = Math.floor(Date.now() / 1000) + sec;
        return this;
    };
    JWS.prototype.expiresAt = function (date) {
        this.claims.exp = Math.floor(date.getTime() / 1000);
        return this;
    };
    JWS.prototype.notValidBefore = function (date) {
        this.claims.nbf = Math.floor(date.getTime() / 1000);
        return this;
    };
    JWS.prototype.issuedBy = function (issuer) {
        this.claims.iss = issuer;
        return this;
    };
    JWS.prototype.intendedFor = function (audience) {
        this.claims.aud = [].concat(audience);
        return this;
    };
    JWS.prototype.useAlghoritm = function (alg) {
        this.header.alg = alg;
        return this;
    };
    JWS.prototype.isBeforeIssueTime = function () {
        if (!this.claims.iat || typeof this.claims.iat !== 'number')
            return false;
        return this.claims.iat > Math.floor(Date.now() / 1000);
    };
    JWS.prototype.isExpired = function () {
        if (!this.claims.exp || typeof this.claims.exp !== 'number')
            return false;
        return this.claims.exp <= Math.floor(Date.now() / 1000);
    };
    JWS.prototype.isIntendedFor = function (aud) {
        if (!this.claims.aud || !Array.isArray(this.claims.aud))
            return true;
        return this.claims.aud.includes(aud);
    };
    JWS.prototype.setHeader = function (header) {
        this.header = __assign(__assign({}, this.header), header);
        return this;
    };
    JWS.prototype.setClaims = function (claims) {
        this.claims = __assign(__assign({}, this.claims), claims);
        return this;
    };
    JWS.prototype.setProvider = function (provider) {
        this.provider = provider;
    };
    JWS.prototype.getHeader = function () {
        return this.header;
    };
    JWS.prototype.getClaims = function () {
        return this.claims;
    };
    JWS.prototype.setRaw = function (raw) {
        var header = JSON.parse(Buffer.from(raw.split('.')[0], 'base64').toString());
        var claims = JSON.parse(Buffer.from(raw.split('.')[1], 'base64').toString());
        this.claims = claims;
        this.header = header;
        this.raw = raw;
    };
    JWS.prototype.sign = function () {
        return __awaiter(this, void 0, void 0, function () {
            var issuedAt, _a, error_1;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        if (!this.provider || !this.provider.sign) {
                            throw new Error('Invalid provider. Make sure it has a sign() method.');
                        }
                        issuedAt = this.claims.iat;
                        _b.label = 1;
                    case 1:
                        _b.trys.push([1, 3, , 4]);
                        this.claims.iat = Math.floor(Date.now() / 1000);
                        _a = this;
                        return [4 /*yield*/, this.provider.sign(this.claims, this.header)];
                    case 2:
                        _a.raw = _b.sent();
                        return [3 /*break*/, 4];
                    case 3:
                        error_1 = _b.sent();
                        this.claims.iat = issuedAt;
                        throw error_1;
                    case 4: return [2 /*return*/];
                }
            });
        });
    };
    JWS.prototype.valid = function () {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                if (!this.provider || !this.provider.valid) {
                    throw new Error('Invalid provider. Make sure it has a valid() method.');
                }
                if (!this.raw) {
                    if (this.header.alg !== JWTAlghoritm.none) {
                        throw new Error('Cannot verify an unsigned token.');
                    }
                    return [2 /*return*/, true];
                }
                return [2 /*return*/, this.provider.valid(this.raw)];
            });
        });
    };
    JWS.prototype.toString = function () {
        if (!this.raw) {
            throw new Error('JWS needs to be signed first.');
        }
        return this.raw;
    };
    JWS.fromString = function (raw, provider) {
        var token = new JWS(provider);
        token.setRaw(raw);
        return token;
    };
    return JWS;
}());
exports.default = JWS;
